{
  "name": "sqs-dlq-failure",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "worker:order": "ts-node --project tsconfig.worker.json src/workers/order-processor.ts",
    "worker:failure": "ts-node --project tsconfig.worker.json src/workers/failure-processor.ts",
    "setup:queues": "ts-node --project tsconfig.worker.json src/scripts/setup-queues.ts",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.600.0",
    "@types/dotenv": "^6.1.1",
    "dotenv": "^17.4.2",
    "next": "^16.2.6",
    "react": "^18",
    "react-dom": "^18",
    "winston": "^3.13.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "ts-node": "^10.9.2",
    "typescript": "^5"
  }
}
