#!/bin/bash

REFRESH_INTERVAL=${1:-5}
PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
SCRIPTS_DIR="$PROJECT_ROOT/scripts"

while true; do
  clear
  echo ""
  echo "??????????????????????????????????????????????????????????????????"
  echo "?        Floci Event Booking - Real-Time Infrastructure         ?"
  echo "?                    (Refreshing every ${REFRESH_INTERVAL}s)                      ?"
  echo "??????????????????????????????????????????????????????????????????"
  echo ""
  echo "??  Last updated: $(date '+%Y-%m-%d %H:%M:%S')"
  echo ""
  
  echo "1??  DYNAMODB TABLES"
  echo "??????????????????????????????????????????????????????????????"
  bash "$SCRIPTS_DIR/show-dynamodb.sh" 2>/dev/null | tail -15
  echo ""
  
  echo "2??  S3 BUCKETS"
  echo "??????????????????????????????????????????????????????????????"
  bash "$SCRIPTS_DIR/show-s3.sh" 2>/dev/null | tail -8
  echo ""
  
  echo "3??  SQS QUEUES"
  echo "??????????????????????????????????????????????????????????????"
  bash "$SCRIPTS_DIR/show-sqs.sh" 2>/dev/null | tail -12
  echo ""
  
  echo "Press Ctrl+C to stop..."
  sleep $REFRESH_INTERVAL
done
